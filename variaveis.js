// variaveis servem para guardar informações

let nome = "Winter Solder"
let idade = 122
let poder = "Braço biônico"

console.log(
    `Nome: ${nome} / Idade: ${idade} / Poder: ${poder}`
)

// É possivél mudar o valor de uma variável a qualquer momento

// No JS existem palavras reservadas que não permitem ser usadas para criar variáveis (class, functions)

/*
    VAR, LET E CONST

var = Não se deve usar mais

let = Valores que podem se alterar

const = Valores constantes que nunca se alteram (Módulos, Objetos)

*/